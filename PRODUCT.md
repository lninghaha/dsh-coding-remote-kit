# Product

## Register

product

## Users

DeepSeek Harness 操作者与开发者。他们在桌面端运行 DSH，并在手机上安全地查看会话、回答问题、审批操作和发送提示；典型场景是离开桌面后仍需保持对当前编码任务的可控访问。

## Product Purpose

提供与 DSH 生命周期相容的端到端加密移动伴侣。成功意味着用户能快速理解连接方式、完成配对、辨认设备和会话，并在网络变化或 DSH 升级时得到明确、可恢复的状态，而不会丢失输入或扩大宿主攻击面。

## Brand Personality

克制、可信、专注。界面应像 DSH 的原生延伸，移动端可以更沉浸，但不能显得像另一套互不相关的产品。

## Anti-references

避免炫技式安全视觉、霓虹终端装饰、一次展示全部网络术语、无上下文的空白面板、依赖颜色才能理解的状态，以及把敏感值或内部标识当作主要信息的界面。

## Design Principles

1. 先说明当前能否安全使用，再提供下一步操作。
2. 连接、配对和授权按用户任务渐进展开，高级网络细节按需显示。
3. 服务端推送不得打断正在进行的输入、焦点或操作。
4. 与 DSH 的不兼容必须可观察、可降级，不以宿主崩溃或静默消失呈现。
5. 安全默认值不可因易用性优化而被放宽。

## Accessibility & Inclusion

所有关键流程支持键盘与屏幕阅读器，提供可见焦点、状态播报、语义化对话框和 reduced-motion；移动端兼顾安全区、窄屏、缩放与触控目标，并避免只靠颜色表达状态。
